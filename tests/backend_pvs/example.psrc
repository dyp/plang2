module A {
    nat a = 0, b = ~a;

    type T = 1 .. 10;

    lemma forall T t. t <= 100;

    array(nat, T) arr = [1: 1, 2: 2];

    array(nat, 1..10, 1..10) arr1 =
        for (int i, j) {
            case (1..5, 1..2): 1
            case (6..30, 3..5): 2
            default: 3
        };

    nat e = arr[1];

    list(nat) l = [[1, 2, 3]];

    set(nat) s = {1, 2 ,3};

    type En = enum(red, green, blue);

    type Lst = list(nat);
}
